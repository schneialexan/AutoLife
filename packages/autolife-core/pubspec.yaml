name: autolife_core
description: Shared AutoLife domain contracts, drift models, and integration plumbing.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  supabase: ^2.10.0

dev_dependencies:
  build_runner: ^2.5.4
  flutter_lints: ^6.0.0
  freezed: ^3.1.0
  json_serializable: ^6.9.5
  path: ^1.9.1
  test: ^1.25.6
