name: autolife_platform
description: "Offline-first sync platform for AutoLife apps — auth, encrypted outbox, sync coordinator, and pluggable per-module gateways backed by Supabase."
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.6.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  supabase_flutter: ^2.8.0
  flutter_secure_storage: ^9.2.2
  connectivity_plus: ^6.1.0
  crypto: ^3.0.6
  path: ^1.9.0
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
