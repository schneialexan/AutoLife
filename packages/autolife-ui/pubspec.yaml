name: autolife_ui
description: AutoLife shared UI and design system (Phase 1.3+).
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.11.0
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
