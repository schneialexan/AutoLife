# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
.cursor/
*.swp

# Env / secrets
.env
.env.*
!.env.example

# Android release signing — never commit keystores or their passwords
*.jks
*.keystore
key.properties

# Local git hooks (install via scripts/setup-git-hooks.*)
.githooks/

# Logs
*.log

# Generic build / dependency output (refined once a stack is chosen — see docs/open-decisions.md)
build/
dist/
out/
node_modules/
.dart_tool/
.packages

# Supabase CLI local state
platform/supabase/.temp/
platform/supabase/.branches/
